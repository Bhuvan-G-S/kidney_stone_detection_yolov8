



 kidney stone detection using yolov8 ( dataset and code included) run in google collab for best results

first clone repository copy paste the required code in google collab change runtime to GPU upload the data set if are doing the training. upload pt file (pre trained file) if you are running the detection code. apply the path to the specified section in code run and test using provided test images or upload your own images.     
